rules:
  unpinned-uses:
    config:
      policies:
        # Keep the release version in a comment, but execute only an immutable
        # commit so a moved tag cannot change CI behavior.
        "*": hash-pin
