# {{ project_name }}

{{ description }}

## Installation

{% if workload == 'api' -%}
```bash
git clone https://github.com/{{ github_username }}/{{ github_repo }}.git
cd {{ github_repo }}
uv sync
```
{%- else -%}
```bash
uv add {{ project_slug }}
```

Or with pip:

```bash
pip install {{ project_slug }}
```
{%- endif %}

## Quick start

{% if workload == 'library' -%}
```python
from {{ module_name }} import greet

print(greet("world"))
```
{%- elif workload == 'cli' -%}
```bash
{{ project_slug }} hello world
```
{%- elif workload == 'api' -%}
{% if framework == 'fastapi' -%}
```bash
uv run fastapi dev src/{{ module_name }}/api.py
```

Then open <http://127.0.0.1:8000/docs> for the interactive API documentation.
{%- else -%}
```bash
uv run flask --app {{ module_name }}.api run --debug
```
{%- endif %}
{%- endif %}

See [Usage](usage.md) for more.
