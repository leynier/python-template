"""RunPod CLI entrypoint; implementation stays inside the package."""

import runpod

from {{ module_name }}.deploy.runpod_handler import handler

if __name__ == "__main__":  # pragma: no cover
    runpod.serverless.start({"handler": handler})
