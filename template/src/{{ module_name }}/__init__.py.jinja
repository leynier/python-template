{{ (description or project_name) | tojson }}

__version__ = "0.1.0"

{% if workload == 'library' -%}
__all__ = ["greet"]


def greet(name: str) -> str:
    """Return a friendly greeting for ``name``."""
    return f"Hello, {name}!"
{%- elif workload == 'cli' -%}
__all__ = ["greet"]


def greet(name: str) -> str:
    """Return a friendly greeting for ``name``."""
    return f"Hello, {name}!"
{%- elif workload == 'api' -%}
__all__ = ["app"]

from {{ module_name }}.api import app
{%- else -%}
__all__: list[str] = []
{%- endif %}
