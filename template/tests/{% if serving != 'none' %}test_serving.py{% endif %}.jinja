"""Tests for the serving adapter."""

from {{ module_name }}.serving import SERVING_ENGINE, main, serving_sdk


def test_serving_sdk_is_installed() -> None:
    assert SERVING_ENGINE == "{{ serving }}"
    assert serving_sdk() is not None


def test_serving_main(capsys) -> None:  # type: ignore[no-untyped-def]
    main()
    assert "Serving engine: {{ serving }}" in capsys.readouterr().out
