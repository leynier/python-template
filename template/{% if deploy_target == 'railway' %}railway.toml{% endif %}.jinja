[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
{% if workload not in ['mcp', 'training'] %}
healthcheckPath = "/health"
healthcheckTimeout = 100
{% endif %}
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
